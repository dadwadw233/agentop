# Agentop - Quickstart Guide

## MVP 功能

✅ **已实现的功能**：

- 🔍 **进程监控**：检测并监控本地运行的 AI agent 进程（Claude Code）
- 📊 **资源追踪**：实时显示 CPU、内存使用情况
- 📝 **日志解析**：解析 Claude Code 的 JSONL 会话日志
- 💰 **使用统计**：Token 使用量、费用估算（今日/本月）
- 🖥️ **TUI 界面**：基于 Textual 的终端用户界面

## 安装

```bash
# 克隆或进入项目目录
cd agentop

# 安装（开发模式）
pip install -e .

# 或者直接安装依赖
pip install textual rich psutil pydantic aiofiles httpx pyyaml platformdirs
```

## 运行

### 方式 1：命令行工具
```bash
agentop
```

### 方式 2：Python 模块
```bash
python3 -m agentop
```

## 测试结果

在你的系统上测试成功：

```
[Claude Code Monitor]
  Active: False (当前没有运行的 Claude Code 进程)
  Sessions today: 0
  Token Usage: 0 tokens
  Cost: $0.00
```

## 当前支持的 Agent

| Agent | 进程监控 | 日志解析 | API 集成 | 状态 |
|-------|----------|----------|----------|------|
| **Claude Code** | ✅ | ✅ | ⏳ | MVP 完成 |
| **Antigravity** | ⏳ | ✅ | ⏳ | quota（beta） |
| **OpenAI Codex** | ✅ | ⏳ | ⏳ | 进程监控 + quota（beta） |
| **GitHub Copilot** | ⏳ | ⏳ | ⏳ | 计划中 |

## TUI 界面预览

```
┌─ Agentop ──────────────────────────────────────────┐
│ 🤖 Agentop - MVP                                    │
├──────────────────────────────────────────────────────────┤
│                                                           │
│ ┌─ CLAUDE CODE ────────────────────── 🟢 Active ───────┐ │
│ │ Processes:  2 running - PID 1234, PID 5678           │ │
│ │ CPU:        3.2% ████░░░░░░░░░░░░░░░░░░               │ │
│ │ Memory:     890 MB                                     │ │
│ │                                                        │ │
│ │ Sessions:   2 active • 12 today                        │ │
│ │                                                        │ │
│ │ Token Usage (Today):                                   │ │
│ │   Input:   145,892 tokens ██████████████░░░░░ 73%    │ │
│ │   Output:   54,231 tokens █████░░░░░░░░░░░░░ 27%     │ │
│ │   Total:   200,123 tokens                             │ │
│ │                                                        │ │
│ │ Cost Today:        $4.23                              │ │
│ │ Cost This Month:   $87.45                             │ │
│ └───────────────────────────────────────────────────────┘ │
│                                                           │
├──────────────────────────────────────────────────────────┤
│ Press Q to quit • R to refresh • ? for help              │
└──────────────────────────────────────────────────────────┘
```

## 数据源

### Claude Code
- **进程检测**：`/Users/xxx/.local/bin/claude`
- **日志路径**：`~/.claude-code/sessions/*.jsonl`
- **监控指标**：
  - Token 使用（input/output）
  - 会话数量（活跃/总数）
  - 费用估算（基于 Claude 定价）

### Codex
- **进程检测**：`codex`
- **监控指标**：
  - 进程 CPU/内存
  - Quota（API，beta）

### Antigravity
- **监控指标**：
  - Quota（API，beta，需要登录）


## 项目结构

```
agentop/
├── agentop/
│   ├── core/              # 数据模型和常量
│   │   ├── models.py      # ProcessMetrics, ClaudeCodeMetrics
│   │   └── constants.py   # Agent 配置、定价信息
│   │
│   ├── monitors/          # 监控器
│   │   ├── process.py     # 通用进程监控
│   │   └── claude_code.py # Claude Code 专用监控
│   │
│   ├── parsers/           # 日志解析
│   │   └── claude_logs.py # JSONL 日志解析器
│   │
│   └── ui/                # TUI 界面
│       ├── app.py         # 主应用
│       └── widgets/       # UI 组件
│
├── pyproject.toml         # 项目配置
└── PROJECT_PLAN.md        # 完整方案
```

## 下一步计划

### Phase 2（未来功能）
- [ ] Copilot 监控支持
- [ ] 配置文件系统（YAML）
- [ ] 历史数据存储（SQLite）
- [ ] Timeline 图表

### Phase 3（增强功能）
- [ ] 告警系统（Quota 预警）
- [ ] 快捷键操作
- [ ] 导出报告（CSV/JSON）
- [ ] 跨平台测试

## 故障排查

### 问题：找不到 Claude Code 进程

**可能原因**：
1. Claude Code 当前未运行
2. 进程检测模式不匹配

**解决方案**：
```bash
# 手动检查 Claude Code 进程
ps aux | grep claude

# 如果进程存在但未检测到，需要调整 constants.py 中的模式
```

### 问题：日志解析返回 0

**可能原因**：
1. 日志目录不存在
2. 今天没有使用 Claude Code

**检查**：
```bash
# 检查日志目录
ls -la ~/.claude-code/sessions/

# 查看最近的日志文件
ls -lt ~/.claude-code/sessions/*.jsonl | head -5
```

## 贡献

欢迎贡献！主要需求：

1. **新 Agent 支持**：添加 Copilot 等监控
2. **跨平台测试**：Linux、Windows 测试
3. **UI 增强**：更多可视化组件
4. **文档**：使用教程、API 文档

## License

MIT
